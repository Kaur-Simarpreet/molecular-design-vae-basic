# Core — required for train_vae.py and serve.py
torch>=1.12.0
numpy>=1.21.0
selfies>=2.1.0
rdkit>=2022.03.1
flask>=2.2.0
flask-cors>=3.0.10
scipy>=1.7.0

# For download_weights.py / upload_weights.py (skip training, get pre-trained)
huggingface_hub>=0.20.0

# Note: RDKit is most reliable via conda
#   conda install -c conda-forge rdkit
# then pip install the rest
